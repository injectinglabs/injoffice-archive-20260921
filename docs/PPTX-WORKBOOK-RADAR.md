# Embedded-workbook radar previews

The explicit workbook-chart preview now includes bounded standard and filled radar charts. It uses the existing read-only embedded-workbook inspection and private resolution boundary. The deck retains its opaque chart; radar values are never attached as literal source data.

The source record retains radar style, XML series sequence, original index/order metadata, explicit stroke/fill, paired axes and each category/value/title reference. The `workbook-radar-v1` result uses exact saved worksheet literal cells through the existing XLSX V2 extractor. Chart caches are recorded as present and ignored, including contradictory cached values. Formula-backed, absent, rich or unsupported cells remain refusals. Package, slide, frame, chart, workbook relationship and digest identities must match; externally constructed or cloned resolved records do not gain private admission.

This slice requires 3–256 common categories, 1–16 series, a unique canonical order permutation without array sorting, explicit RGB paint, bounded line widths, explicit reciprocal linear axes and values within the authored scale. Standard radar requires no fill; filled radar requires an explicit fill. Markers, labels, point overrides, theme/default/style-part paint, hidden-cell filtering and out-of-range radial clipping remain outside admission. Existing workbook resource, record, reference and point budgets are unchanged.

Both authority paths use the same profile-free radial geometry: categories start at the top and run clockwise, category/value reversals are retained, and value-axis spokes paint before standard outlines and after filled polygons. Host circular fitting and bounded angular rounding remain disclosed. The complete stroke hull plus the separate 1 EMU numerical envelope is checked before clipping and carried through source-anchored physical-frame qualification. Direct graphic-frame rotation/reflection follows that existing anchor policy; source metadata is retained, and grouped physical scale/anchor changes remain qualified.

`NativeRadarData` is a geometry data interface, not an admission token. Its validator enforces radial data constraints. Literal wrappers still validate the complete literal profile; workbook paths still require the private resolved workbook token. The geometry module does not issue either authority.

`TestNativeWorkbookRadarFixtures` creates 16 real strict/transitional, standard/filled, plain/own-rotated/grouped/reversed PPTX packages with actual embedded XLSX files and misleading category/value caches. It also creates actual formula, outside-scale and missing-cell negatives. The original literal fixtures and external Office evidence remain unchanged. Worker tests compare Go and WASM source/inspection output, decode actual embedded XLSX bytes, check exact lexical values and XML order, invoke the production framed worker, assert default-off behavior and stale/malformed transport refusal, and preserve source bytes.

The dedicated browser workflow runs the actual helper, worker and production SVG component under Node 24 and Go 1.23. It checks exact DOM path data/paint/order, fully visible measured SVG bounds, nondegenerate series and same-region hide-series pixel controls, restoring inline visibility before failure evidence. It always uploads source packages, extracted decks, worker responses, DOM identities and clipped screenshots. Local Chrome is not retried where startup is denied; browser qualification requires the workflow result and artifact review.

No numerical, input or WASM artifact ceiling is increased by this slice. No new PowerPoint export qualification is claimed: the retained radial and graphic-frame geometry policies are reused with separately qualified workbook cell authority.
